#include <stdio.h>
#include <stdlib.h>

int main()
{
    int graf[50][50];
    int i, n, j, n2,p=0;

    printf("Nr de nodur: ");
    scanf("%d", &n);

    for(i=1;i<=n;i++)
     for(j=1;j<=n;j++)
     {
         scanf("%d",&graf[i][j]);
     }

     //parcurgem matricea pe coloane
     for(j=1;j<=n;j++)
      {
          n2=0;//contor
      for(i=1;i<=n;i++)
      {
       if (graf[i][j]==0)
       n2++;
      }

      if (n2==n)
      {
      printf("Graful NU este CONEX");
        p=1;
      }
      }
      if (p==0)
      printf("Graful este CONEX");



    return 0;
}
