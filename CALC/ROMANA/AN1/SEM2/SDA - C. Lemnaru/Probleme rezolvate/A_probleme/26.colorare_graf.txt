#include <stdio.h>
#include <stdlib.h>
int a[100][100], x[100];
int m,n;// nr de culori(1, 2, 3, ... m) si nr de noduri
void citire_graf()
{
        int i,j;
        FILE *f;
        f=fopen("C:\\Users\\ady\\Desktop\\documente cu de toate pt scoala\\an 1 sem 2 2009\\SDA Ignat\\colorare_graf_rec\\graf.txt","r");
        if (f==0) printf("nulllll");
        fscanf(f,"%d",&m);
        fscanf(f,"%d",&n);
        for (i=0;i<n;i++)
             { printf("\n");
             for (j=0;j<n;j++)
              {fscanf(f,"%d", &a[i][j]);
               printf(" %d ",a[i][j]);
              }
             }

}

int bun (int k)
{
    int i,j;
    for (i=0; i<k; i++)
            if (a[k][i]==1 && x[k]==x[i] )
                return 0;
    return 1;
}
void colorare(int k)
{
       int j;

       for (j=1;j<=m;j++)
       {
            x[k]=j;
            if (bun(k))
                if (k<n-1)
                {
                colorare(k+1);
                }
                else{
                    printf("solutie");
                    int h=0;
                    for (h;h<n;h++)
                        printf(" %d ", x[h]);
                }
       }
}
int main()
{
    citire_graf();
    colorare(0);
    printf("Hello world!\n");
    return 0;
}
