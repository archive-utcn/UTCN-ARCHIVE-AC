
DATA SEGMENT PARA PUBLIC 'DATA'	
	NUM1	DB	5
	NUM2	DB	1
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
	ASSUME CS:CODE, DS:DATA
START PROC FAR
	PUSH 	DS
	XOR 	AX,AX
	PUSH 	AX
	MOV 	AX,DATA
	MOV 	DS,AX
	XOR		BX,BX
	XOR		CX,CX
	
	MOV		AX,4
	MOV		BX,8
	
	MOV 	DX,AX
	SHL		DX,2
	MOV		CX,AX
	SHL		CX,1
	ADD		DX,CX
	ADD		DX,AX
	MOV		AX,DX
	
	MOV		CX,BX
	SHL		BX,1
	SHR		CX,3
	SUB		AX,BX
	SUB		AX,CX
	RET
START ENDP
CODE ENDS
END START

;	MOV		CX,AX
;	SHL		AX,3
;	SUB		AX,CX
;	PENTRU 7 ORI CEVA