
PRINT_TEXT MACRO TEXT, LG
	LOCAL PRINTARE
	
	PUSH AX
	PUSH BX
	PUSH CX
	PUSH DX
	
	MOV BX, 0
	MOV CX, LG
	
	PRINTARE:
		MOV AH, 02H
		MOV DL, TEXT[BX]
		INT 21H
		INC BX
		LOOP PRINTARE
	POP DX
	POP CX
	POP BX
	POP AX
	ENDM

		