; REZ = AL*NUMI1 + NUM2*AL + BL
DATA SEGMENT PARA PUBLIC 'DATA'
NUM1 DB 1
NUM2 DB 1
REZ2 Dw ?
REZ Dw ?
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
	ASSUME CS:CODE, DS:DATA
	PUSH DS
	XOR AX,AX
	PUSH AX
	MOV AX, DATA
	MOV DS, AX
;the main program instructions
	MOV AL, 13
	MOV BL, 15
	
	MOV DL, AL
	MUL NUM1
	ADD REZ2, AX
	
	MOV AL, DL
	MUL NUM2
	ADD REZ2, AX
	
	MOV AL, BL
	ADD REZ2, AX
	
	
	MOV AX, REZ2
	
	
	;mov dx, REZ
	;sub dx, '0'
	;mov ax,09h
	;int 21h
	
RET
START ENDP
CODE ENDS
END START
	
	