#include <stdio.h>

int main(){

    int i=0;
    char array[] = "computer";
    char *ptr_array;
    ptr_array = array;

    printf("%s\n", array);
    printf("%c %c %c %c", array[1], *(array+1), ptr_array[1], *(ptr_array+1));
    while (array[i]!='\0') {
    printf("\n %c", array[i]);
    i++;
    }
    i=0;
    while (ptr_array[i]!='\0') {
    printf("\n %c", ptr_array[i]);
    i++;
    }
    i=0;
    while (*(ptr_array +i)!='\0') {
    printf("\n %c", ptr_array[i]);
    i++;
    }
    i=0;
    while (*(array +i)!='\0') {
    printf("\n %c", ptr_array[i]);
    i++;
    }

    return 0;


}
